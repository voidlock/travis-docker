env:
  global:
    SLIRP_PORTS="6379"

install:
  - source bootstrap.sh

before_script:
  - docker version
  - docker pull redis:latest

script:
  - docker run -d -p 6379:6379 redis:latest
  - redis-cli info

notifications:
  email: false
